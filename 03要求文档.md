# 03 要求文档 · 外场样例实测后的优化建议

> 承接 [`02要求文档.md`](02要求文档.md)、[`02结果文档.md`](02结果文档.md)。  
> **本阶段主诉（2026-09-02 修订）**：**干声识别**——**正常说话**与**无伴奏清唱**均可自动转写；不做人声分离；**mp4/口型演示暂缓**。

---

## 〇、需求边界

| 场景 | 必达 | 说明 |
|---|---|---|
| **正常说话**（口播/对白/独白） | **✅ 必达** | 默认 `neutral` profile |
| **歌曲清唱**（无伴奏、人声为主） | **✅ 必达** | `--singing` + **`--no-gold` 为 ASR 验收轨** |
| 带明显伴奏成曲 | ❌ 非必达 | 不做人声分离则不承诺 |
| mp4 / 口型视频 / F9 | ❌ **暂缓** | 与 ASR 无关，结题后排期 |

**清唱定义**：源音频以人声演唱为主，无明显鼓点/和弦盖过人声；允许气口、颤音、拖腔。

**验收铁律**：有 `.gold.json` 时默认**跳过 Whisper**；测 ASR 必须 `--no-gold`。

---

## 一、02 已完成 vs 本轮差距

| 能力 | 说话 | 清唱 ASR (`--no-gold`) |
|---|---|---|
| 可懂转写 | ✅ 基本达标 | ✅ 两样例均过冲刺（90.9% / 100%） |
| 全链路翻译/克隆 | ✅ | gold 旁路 ✅；纯 ASR 链 ⚠️ |
| 幻觉 prompt | ✅ O4 已修 | ✅ G1 护栏 |

---

## 二、问题拆解（ASR 向）

### 说话（neutral）

- 专名脆弱（千夏等）→ **G4 专名表 + prompt 热词**
- 主题句 prompt 污染 → 已改中性 prompt

### 清唱（singing）

- Whisper 说话域偏置 → singing profile + beam↑
- 近音错字 → **S5 corrections + lyrics_hint 对齐**
- 切段过碎 → S2 参数（6s 段、merge 0.8s）
- 误判多人 → `--assume-single-speaker`

---

## 三、优化清单

### P0 · ASR 必达（本阶段全部精力）

- [x] S1–S4 清唱模式 / 样例 / gold
- [x] G1 幻觉护栏
- [x] **真 ASR 轨** — `--no-lyrics-hint`（不用歌名/歌词/歌曲纠错）
- [x] **引擎对比** — Whisper **77%** ≫ FunASR **36%** → **默认保持 Whisper**
- [x] **FunASR** — 本地可跑；清唱不优，不切默认
- [x] **通用同音** — `generic_homophone_pairs`（真 ASR 可用）
- [ ] **通用 LLM 修错** — 本机无本地 Qwen，**暂跳过**
- [x] **通过线 ≥70%** — 当前样例 **77% ✅**
- [x] **S5 后处理**（演示轨，含 hint 时 100% 关键词）
- [x] **G4 口播专名** — `lexicon_broadcast.txt`
- [x] **C2 量化** — `score_singing_asr.py --no-lyrics-hint`

**真 ASR（如果爱忘了 60s）**

| 档 | 标准 | 当前 |
|---|---|---|
| **必达** | 无 hint 关键词 ≥**70%** | **90.9% / 100% ✅** |
| **冲刺** | 无 hint 关键词 ≥**85%** | **两样例均过 ✅** |
| 演示轨 | 允许 hint | 不计 ASR KPI |

### P1 · 全链路（ASR 达标后）

- [ ] G3 空译 / length 门禁（已有，随 ASR 重跑验证）
- [ ] G7 MP3 入库校验

### 暂缓（不做）

- [ ] F9 mp4 / 口型演示
- [ ] G8 带伴奏成曲 ASR
- [ ] 人声分离

### P3 · 结题材料（ASR 达标后排期）

- [ ] F3 MOS、F8 四类指标表、F10/F11 demo_pack

---

## 四、验收通过线

| 检查项 | 标准 |
|---|---|
| **说话 ASR** | `real_dialog_02` / 口播样例：语义可懂，无 prompt 幻觉 |
| **清唱 ASR（`--no-gold --no-lyrics-hint`）** | 关键词命中率 **≥70%** 且 bigram≥0.35；或 ≥3 人句意「可用」 |
| 清唱 ASR · 冲刺 | 无 hint 关键词 **≥85%** |
| 清唱全链路（gold） | sync≥0.65；length∈[0.8,1.2]；无空译 |
| 演示轨（有 hint） | 不计入真 ASR KPI |

---

## 五、复现命令

```bash
# 说话
python run_pipeline.py --input data/real_dialog_02.wav -t en --device cpu

# 清唱 · 真 ASR KPI（不用歌词）
python run_pipeline.py --input data/singing_ruguaiawang_16k.wav \
  --singing --assume-single-speaker --no-gold --no-lyrics-hint -t en --device cpu

python scripts/score_singing_asr.py \
  --gold data/singing_ruguaiawang_16k.gold.json \
  --audio data/singing_ruguaiawang_16k.wav --no-gold-asr --no-lyrics-hint --device cpu

# 引擎对比 Whisper vs FunASR（均无 hint）
python scripts/compare_singing_asr_no_hint.py data/singing_ruguaiawang_16k.wav

# 可选：通用 LLM 修同音（需 --translate-engine local 或 API）
python run_pipeline.py ... --no-lyrics-hint --generic-asr-refine --translate-engine local

# 演示轨（允许 hint，听感更好，勿当真 ASR 成绩）
python run_pipeline.py --input data/singing_ruguaiawang_16k.wav \
  --singing --no-gold -t en --device cpu
```

---

## 六、关键文件

| 文件 | 方向 |
|---|---|
| `src/asr/asr_profiles.py` | singing/neutral profile |
| `src/asr/singing_corrector.py` | **S5 后处理** |
| `src/asr/whisper_asr.py` | Whisper + 段级 corrections |
| `src/integration/pipeline.py` | `_post_refine_asr` |
| `config/default.yaml` | singing_corrections、lexicon |
| `data/lexicon_broadcast.txt` | G4 专名 |
| `scripts/score_singing_asr.py` | ASR 评分 |
