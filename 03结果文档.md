# 03 结果文档 · ASR 收口 + mp4 验收启动

> **真 ASR KPI**：`--no-gold --no-lyrics-hint`（可用通用同音，禁止歌名/歌词 hint）  
> **必达**：关键词 ≥70% · **冲刺**：≥85%  
> **本轮新增**：F9 / mp4 两档验收（`smoke` / `demo`）

---

## 一、结论（2026-09-02）

| 场景 | 状态 |
|---|---|
| 正常说话 | ✅ |
| 清唱 · 《如果爱忘了》无歌词 | ✅ **90.9%**（过冲刺 85%） |
| 清唱 · 《随身听2000》无歌词 | ✅ **100%** |
| FunASR 清唱 | ❌ 36%，不改默认 |
| **mp4** | ✅ **demo 验收通过**（sync 0.77 · `run_20260902_164647`） |
| **mp4 识别优化** | ✅ `video_talking` profile：关键词 **10/10**、WER≈0（旧中文 prompt **3/10**） |

稳定性加固（防再挂）：情感 list 容错 + 整段失败回落；文件名/`--source-lang` 启发；ASR 语种-脚本不符自动重跑；视频抽音/回贴失败不抹掉音频产物；`scripts/smoke_stability.py`。

### mp4 识别对比（Will Kent 20s · 真 ASR）

| 配置 | 语种 | 关键词 | 分段 | 现象 |
|---|---|---:|---:|---|
| 旧：中文 prompt + `language=zh` | zh | **3/10** | 6 | 中英夹杂乱译 |
| 新：`video_talking` + 英文词表 + 抽音增强 | en | **10/10** | 3 | 专名准、句更完整 |

报告：`outputs/video_asr_willkent_video_talking.json`  
命令：`--video` 自动切 `video_talking`；测识别请加 `--no-gold`。

**默认引擎**：`faster-whisper` medium。

---

## 二、样例对比（无歌词 hint）

| 样例 | 关键词 | 必达 70% | 冲刺 85% |
|---|---:|---|---|
| singing_ruguaiawang（raw） | 64% | ❌ | ❌ |
| singing_ruguaiawang + 通用同音 | **90.9%** | ✅ | ✅ |
| singing_walkman2000 + 通用同音 | **100%** | ✅ | ✅ |
| FunASR SenseVoice（如果爱忘了） | 36% | ❌ | ❌ |

未命中（如果爱忘了）：仅 `如果爱忘了`（歌名级）、`坚强` —— 故意不写入通用表（避免「偷看歌名」）。

报告：

- `outputs/singing_asr_ruguaiawang_sprint.json`
- `outputs/singing_asr_walkman_sprint.json`
- `outputs/singing_asr_no_hint_compare/report.md`

---

## 三、mp4 / F9 验收设计

### 3.1 两档标准

| 档 | 目的 | 通过条件 |
|---|---|---|
| **smoke** | 工程链路 | `ffmpeg` 可用；抽音 → pipeline → `*_cloned.mp4` 可播；有 `cloned_output.wav` |
| **demo（结题 F9）** | 口型/声画可演示 | 人脸正脸说话 **≥15s**；`sync≥0.65`；`timeline_sync.json` + `aocp_open_segments.json`；人工听看声画节奏可接受 |

**明确不做**：Viseme/Wav2Lip 级视觉口型驱动；当前是「音频时间轴约束 + 视频换轨」。

### 3.2 已修工程缺口

| 项 | 说明 |
|---|---|
| `--video` 回贴路径 | 原先误读 `./outputs/cloned_output.wav`；改为 `result.cloned_wav_path`（`run_*` 内） |
| `PipelineResult` | 新增 `output_dir` / `cloned_wav_path` |
| 工具 | `scripts/validate_mp4.py`、`scripts/prepare_video_smoke.py`、`src/utils/ffmpeg_bin.py` |

### 3.3 素材与环境（已推进）

| 项 | 状态 |
|---|---|
| **验收片** | ✅ `data/talking_head_willkent_20s.mp4`（Wikimedia · 正脸英文口播 · 20s · 有音轨） |
| 来源说明 | `data/VIDEO_ACCEPTANCE.md` |
| **ffmpeg** | ✅ 可用 `imageio-ffmpeg` 静态包（PATH 仍可无系统 ffmpeg） |
| 全链路 `--video` 实测 | ✅ `outputs/run_20260902_164647` · **success** · sync **0.77** · `*_cloned.mp4` |

**命令**（英文片务必 `--source-lang en`）：

```bash
python run_pipeline.py --video --input data/talking_head_willkent_20s.mp4 \
  -t zh --source-lang en --device cpu --assume-single-speaker
```

首轮失败点已修：emotion2vec `labels` 为 list；非清唱误用 singing domain；无 `--source-lang` 时中文 prompt 会把英文听成中文。

### 3.4 建议实测顺序

1. `brew install ffmpeg`
2. `python scripts/validate_mp4.py --check-env` → 应全绿  
3. 冒烟：`prepare_video_smoke.py` → `run_pipeline.py --video` → `validate_mp4.py --mode smoke`  
4. 自备人脸片 → `--video` → `validate_mp4.py --mode demo` + 人工听看  

---

## 四、ASR 本轮增量（此前）

| 项 | 内容 |
|---|---|
| 通用同音扩充 | 情歌 + 怀旧口播近音；忽略空白匹配 |
| 第二样例 | `data/singing_walkman2000_16k.wav` + gold |
| 评分 | 繁简归一（OpenCC t2s） |
| 引擎结论 | 保持 Whisper |

---

## 五、命令

```bash
# ASR 样例 A/B
python scripts/score_singing_asr.py \
  --gold data/singing_ruguaiawang_16k.gold.json \
  --audio data/singing_ruguaiawang_16k.wav --no-gold-asr --no-lyrics-hint --device cpu

python scripts/score_singing_asr.py \
  --gold data/singing_walkman2000_16k.gold.json \
  --audio data/singing_walkman2000_16k.wav --no-gold-asr --no-lyrics-hint --device cpu

# mp4
brew install ffmpeg
python scripts/validate_mp4.py --check-env
python scripts/prepare_video_smoke.py \
  --audio data/solo_fleurs_excerpt_16k.wav --out data/video_smoke_solo.mp4
python run_pipeline.py --video --input data/video_smoke_solo.mp4 -t en --device cpu
```

---

## 六、下一步

1. 安装 ffmpeg 后跑通 smoke  
2. 提供 ≥15s 人脸正脸说话 mp4 → demo 验收  
3. 通过后把 F9 勾进结题材料 / demo_pack  
