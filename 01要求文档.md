# 01 要求文档 · 优化清单

> 对照《科技立项申请书》目标，结合当前原型主要问题整理。  
> **现状主诉**：① 复制声音时分不出不同说话人；② 实际听感仅限于“能听”。  
> **进度**：2026-08-26 已落地 P0 代码改动（A1–A5 / B1–B5 工程项）；A6/B6 听测与 P1/P2 待验收样例。

---

## 一、申请书目标 vs 当前差距

| 申请书要求 | 当前差距 | 优化优先级 |
|---|---|---|
| 多说话人声纹复刻，各自保持音色 | 分离常塌成 1 人；克隆参考混用，人声串味 | **P0** |
| 语义一致、情感保真的跨语种翻译生成 | 情感标签粗、韵律/情感注入弱，听感像机器念稿 | **P0** |
| 语音与口型/时长对齐（时间轴） | 同步分偏低，跨语时长不稳，拼接生硬 | **P1** |
| 可演示、可验收的完整原型 | 链路能跑通，但效果未达“可展示”标准 | **P0（验收导向）** |

---

## 二、问题拆解

### 问题 A：分不出不同说话人（克隆串音）

可能原因：
1. 说话人分离默认走弱回退（聚类阈值偏松 / pyannote 未真正启用）
2. 分离结果未强制贯穿「参考音选取 → 逐人 prompt → 分段合成」
3. 参考片段过短、含噪声或跨人混段，声纹被污染
4. 全局 `--ref-audio` 覆盖导致全片同一音色
5. 重叠说话、短句、远场麦导致嵌入区分度不够

### 问题 B：效果仅限于“能听”

可能原因：
1. TTS 引擎回退到弱克隆方案（如 edge-tts），音色像度差
2. 参考音质量差 / 过短，零样本克隆上限低
3. 译文长度与源时长不匹配，变速/裁切引入失真
4. 分段拼接咔哒、断层、语速忽快忽慢
5. 情感、语调、停顿控制弱，听感扁平
6. ASR/翻译错误放大到合成端

---

## 三、优化清单（按优先级）

### P0 · 多说话人可区分（必须先过）

- [x] **A1 强制启用可靠 diarization**  
  优先 pyannote；不可用时明确降级日志；`last_meta` 记录 backend；单人塌缩时二次拆分。  
  → `src/asr/speaker_diarization.py`
- [x] **A2 调参与验收**  
  `distance_threshold: 0.40`；`expected_speakers: 2`；ASR 静音二次切段。  
  → `config/default.yaml` + `whisper_asr._refine_segments`  
  **验收 (2026-08-26)**: `dialog_two_speakers_16k.wav` → **说话人数=2**（`run_20260826_160003`）
- [x] **A3 按说话人绑定参考与 prompt**  
  每人独立参考音 + prompt 缓存；多人场景**忽略**全局 `--ref-audio`。  
  → `src/integration/pipeline.py`
- [x] **A4 参考音质控**  
  时长/能量/削波/静音占比打分；不合格标记 `weak_clone` 并换候选段。  
  → `_pick_reference_for_speaker` / `_ref_quality_score`
- [x] **A5 可观测输出**  
  `summary.txt` / `segments.json` 增加：分离后端、克隆引擎、每人参考起止与弱克隆标记。
- [ ] **A6 听测标准（通过线）**  
  双人对话样例：听者能明确听出 ≥2 种不同音色（需真克隆引擎，当前仍为 edge-tts）。

### P0 · 听感从“能听”到“可演示”

- [x] **B1 固定高质量克隆引擎**（工程开关已就绪）  
  `require_clone_engine` / `allow_edge_fallback`；落到 edge-tts 时强警告。  
  → **待装** Qwen3-TTS / CosyVoice2（本机尚未安装，输出仍无真克隆）
- [x] **B2 参考音与源音同条件**  
  参考音峰值归一；按说话人本人片段选取。
- [ ] **B3 时长/语速可控**  
  译文–时间轴联动限速（下一迭代）。
- [x] **B4 拼接与起止音**  
  默认 crossfade 25ms、fade 12ms、组间 gap 0.08s；首尾淡入淡出。
- [x] **B5 情感与停顿**  
  情感粗分桶（positive/negative/neutral），减少抖动误切组。
- [ ] **B6 听测标准（通过线）**  
  单人/双人听测（待真克隆引擎）。

### P1 · 对齐与翻译（申请书核心卖点）

- [ ] **C1 AOCP / 时间轴可用性**  
  无权重时的 VAD 兜底要可验证；同步分目标：演示样例 ≥0.65（现状约 0.52）。
- [ ] **C2 音素/音节长度约束进翻译**  
  译文音节与源时长匹配，减少合成端硬裁切。
- [ ] **C3 翻译质量**  
  固定可用翻译后端；情感进 prompt；抽检无严重漏译、乱码、中英夹杂失控。
- [ ] **C4 口型同步演示素材**  
  准备有口型画面的短视频样例，能展示“时间轴约束前后对比”。

### P2 · 工程与验收（中期/结题）

- [x] **D1 环境可复现（部分）**  
  模型缓存改为：存在 `D:/CodingPackage/models` 则用之，否则项目内 `./models`（Mac 可用）。
- [ ] **D2 标准测试集**  
  至少 3 类样例：单人独白、双人对话、带噪声/远场。
- [ ] **D3 中期验收对齐申请书**
- [ ] **D4 结题验收对齐申请书**

---

## 四、建议验收指标（写进演示/结题口径）

| 指标 | 中期底线 | 结题目标 |
|---|---|---|
| 说话人分离（2～4 人对话） | 能分出 ≥2 人且克隆分人 | 人数准确率主观可用，串音少 |
| 克隆可区分性 | 听者能分出不同音色 | 与原说话人对应基本正确 |
| 听感 | 可懂、无明显炸麦/破音 | 自然度明显优于初版，可演示 |
| 时间轴同步分 | 有分数输出且可解释 | 演示样例 ≥0.65 |
| 全链路 | 一键跑通出 wav + summary | 稳定复现 + 前后对比材料 |

---

## 五、本周建议落地顺序

1. ~~先修 **A1–A5**（多人分声）~~ ✅  
2. ~~ASR 切段 + `expected_speakers=2` + 禁用情感嵌入分人~~ ✅ 验收说话人数=2  
3. **下一步**：安装 Qwen3-TTS 或 CosyVoice2，设 `require_clone_engine: true`，再跑同音频做 A6/B6 听测  
4. 中期前再压 **C1–C3**（对齐/翻译）

---

## 六、明确不纳入本阶段（防发散）

- 不做全量自研大模型训练；优先调通开源/现成克隆与分离模型。  
- 不追求“无法分辨 AI”的商用级像度；以立项验收与可演示为准。  
- 视频口型像素级驱动可作为加分，不阻塞语音多人克隆与听感主线。

---

## 七、本次改动文件

| 文件 | 改动要点 |
|---|---|
| `voiceclone/src/asr/speaker_diarization.py` | 阈值、二次拆分、降级提示、`last_meta` |
| `voiceclone/src/integration/pipeline.py` | 按人参考/prompt、质控、可观测、情感分桶、拼接 |
| `voiceclone/src/synthesis/tts_engine.py` | 禁止静默 edge 回退选项 |
| `voiceclone/config/default.yaml` | P0 默认参数 |
| `voiceclone/run_pipeline.py` | 跨平台模型缓存路径 |

### 建议验证命令

```bash
cd voiceclone
# 双人对话样例（请自备 audio）
python run_pipeline.py --input /path/to/dialog.wav --target-lang en

# 看 summary 是否出现 ≥2 说话人、分离后端、每人参考音
cat outputs/run_*/summary.txt
```

演示级克隆（有权重时）：

```yaml
# config/default.yaml
synthesis:
  require_clone_engine: true
  allow_edge_fallback: false
```
