# 01 结果文档 · 阶段记录与后续优化

> 对照 [`01要求文档.md`](01要求文档.md) 与《科技立项申请书》目标。  
> **记录截止**：2026-08-27  
> **验收样例**：`data/dialog_two_speakers_16k.wav`（双人中文对话，约 16s）

---

## 一、阶段结论（一句话）

**2026-08-27 再验收（run_155909，~50s）：ASR 碎句合并生效（8→6）、无 Taiqing、Google 译通顺、sync=0.78、Qwen3 克隆。** 仍待：真实录音听测；分离首轮偶发塌缩。

---

## 二、关键运行对比

| 指标 | 08-26 | 08-27 Qwen 午 | 08-27 午后 | **08-27 晚 ★** |
|---|---|---|---|---|
| run | 160003 | 105928 | 154157 | **155909** |
| 说话人数 | 2 | 2 | 2 | **2** |
| 分离后端 | +retry | +retry | f0+turn | **spectral+retry+turn** |
| degraded | True | True | False | **False** |
| 克隆引擎 | edge | qwen3 | qwen3 | **qwen3** |
| supports_clone | False | True | True | **True** |
| batch_mode | — | — | True | **True** |
| 翻译 | 短语表 | 短语表 | google 8/8 | **google 6/6** |
| ASR 段数 | — | — | 8（碎句） | **6（已合并）** |
| 输出时长 | 2.1s | 16.6s | 19.2s | **17.8s** |
| length_ratio | 0.05 | 0.03 | 1.29 | **1.21** |
| 同步分 | 0.50 | 0.63 | 0.78 | **0.78** |
| 全链路耗时 | ~26s | ~80min | ~61s | **~50s** |

★ 试听：`outputs/run_20260827_155909/cloned_output.wav`

---

## 三、最新一次完整结果（run_20260827_155909）

### 3.1 运行摘要

```
源语言: zh -> 目标语言: en
源音频时长: 15.8s | 输出音频时长: 17.8s
ASR 分段数: 6（phrase-merge 8→6）| 说话人数: 2 | 克隆分组数: 4
分离后端: spectral_cluster+retry_split+turn_correct (degraded=False)
克隆引擎: qwen3_tts | supports_clone=True | batch_mode=True
时间轴: 6 说话段 | 同步分 0.78
源音节: 58 | 目标音节: 70 | 长度比: 1.21
翻译: google/zh-CN；无 Taiqing 误译
全链路: ~50s
```

### 3.2 说话人参考音

| 说话人 | 参考时间段 | 弱克隆 | 说明 |
|---|---|---|---|
| SPEAKER_00 | 5.10s – 8.70s | 否 | 可用 |
| SPEAKER_01 | 1.14s – 3.92s | 否 | 可用 |

### 3.3 分段与译文（节选）

| # | 源文本（摘要） | 译文（摘要） | 后端 |
|---|---|---|---|
| 1 | 你好 | Hello | google/zh-CN |
| 2 | 今天我們來討論…進度 | Today we will discuss the progress of this project | google/zh-CN |
| 3 | 目前語音克隆還分太清不同說話人 | At present, voice cloning cannot distinguish between different speakers | google/zh-CN |
| 4 | 聽感也一般 | The listening experience is also average | google/zh-CN |
| 5 | 那我們先把說話人分離坐穩 | Then let's separate the speakers and sit down first | google/zh-CN |
| 6 | 再提升克隆質量 | Improve cloning quality | google/zh-CN |

> **ASR 碎句合并**：`_merge_broken_phrases` 将「還分|太清」「把說|話人」合并；**Taiqing 已消除**。  
> **O7**：sync=**0.78**；对比图 `outputs/o7_sync_before_after.png`。

### 3.4 已达成（相对立项初版）

| 申请书能力 | 状态 | 证据 |
|---|---|---|
| 语音识别 + 分段 | ✅ | faster-whisper + 碎句合并，6 段 |
| 说话人分离 | ⚠️ 部分 | 人数=2，首轮偶发塌缩靠 retry |
| 跨语种翻译 | ✅ O5 | google；合并后译文通顺 |
| 时间轴 / AOCP | ✅ O7 | **sync=0.78** ≥0.65 |
| 声纹复刻合成 | ✅ 工程级 | Qwen3-TTS 0.6B-Base |
| 按人独立克隆 | ✅ | 每人独立参考 + prompt |
| 可观测输出 | ✅ | summary / segments.json 完整 |

### 3.5 仍存在的问题

1. **说话人标签不准**：A/B 轮次与真实说话人不完全一致。  
2. **分离不稳定**：首轮偶发 1 人 → retry；未装 pyannote。  
3. **像度有限**：0.6B + CPU/MPS；未试 1.7B。  
4. **情感/韵律弱**：多为 neutral。  
5. **length_ratio=1.21**：略超 1.2，可再收紧（O6）。  
6. **真实录音未测**（O8）。

---

## 四、环境与依赖（当前）

| 项 | 配置 |
|---|---|
| 项目路径 | `voiceclone/` |
| Python | **3.11**（`.venv`，系统 3.9 不可用 qwen-tts） |
| TTS | `qwen-tts 0.1.1`，模型 `Qwen3-TTS-12Hz-0.6B-Base` |
| 配置 | `synthesis.engine: qwen3_tts`，`require_clone_engine: true` |
| 模型缓存 | `models/huggingface`（约 3GB+） |
| 安装脚本 | `scripts/setup_qwen3.sh` |

**推荐运行命令：**

```bash
cd voiceclone
source .venv/bin/activate
export HF_HOME="$(pwd)/models/huggingface"
pip install deep_translator opencc-python-reimplemented
python run_pipeline.py --input data/dialog_two_speakers_16k.wav --target-lang en --device cpu
```

**产物路径：**

- 音频：`outputs/run_20260827_105928/cloned_output.wav`
- 摘要：`outputs/run_20260827_105928/summary.txt`
- 分段：`outputs/run_20260827_105928/segments.json`

---

## 五、相对 01要求文档 的完成度

| 模块 | 要求文档项 | 状态 |
|---|---|---|
| P0 多人分声 | A1–A5 | ✅ 代码完成 |
| P0 多人分声 | A6 听测 | ⚠️ 人数=2 通过，标签准确度未过 |
| P0 听感 | B1 真克隆引擎 | ✅ Qwen3 已装 |
| P0 听感 | B2–B5 | ✅ 工程项完成 |
| P0 听感 | B6 听测 | ⚠️ 可演示底线，像度待提升 |
| P1 对齐/翻译 | C3 翻译 | ✅ O5 完成 |
| P1 对齐/翻译 | C1 同步分 | ✅ O7 全链路 sync=0.78 |
| P1 对齐/翻译 | C2/C4 | ⚠️ 部分 |
| P2 验收 | D2–D4 | ❌ 测试集与结题材料未完成 |

---

## 六、新的优化意见（下一阶段）

> 按**投入产出比**排序；面向中期验收与可重复演示。

### P0 · 必做（1–2 周内）

- [x] **O1 补全 venv 依赖**（2026-08-27）  
  已装 `deep_translator` / `nltk` / `edge-tts` / `opencc-python-reimplemented`；NLTK 数据在 `models/nltk_data`。

- [x] **O2 说话人分离首轮稳定**（部分，2026-08-27）  
  启用 `prefer_f0` + `f0_min_spread_hz=15`；冒烟仍常走 spectral→retry，但 **turn_correct 后 degraded=False**。  
  pyannote 仍未装（需 HF token）。

- [x] **O3 参考音与 ASR 段对齐**（2026-08-27）  
  新增 `_correct_turn_taking`：静音话轮 + F0/多数票 + 相邻强制交替。  
  冒烟标签示例：`00,01,00,01,00,00`（较此前几乎全是 01 更合理）。  
  配置：`turn_taking_correct: true`，`turn_gap_seconds: 0.35`。

- [x] **O4 推理加速（工程项）**（2026-08-27）  
  确认 Qwen `clone_batch` + prompt 复用路径；summary 写 `batch_mode`。  
  修正 Mac 误用项目内 `D:` 目录作 HF 缓存。  
  **耗时目标 <15min 尚未验收**（需再跑全链路）。

### P1 · 效果提升（中期前）

- [x] **O5 翻译质量（C3）**（2026-08-27）  
  `llm_translator`：繁→简（OpenCC + 常用字回退）→ Google(`zh-CN`/`auto`) → MyMemory → 短语表兜底；加强坏结果检测。  
  冒烟 6 句均走 `google/zh-CN`（含非短语表句「机器学习可以用于语音识别。」）；短语表仅作离线兜底。

- [ ] **O6 时长/音节约束（C2 + B3）**  
  已改为按逐段拼接算 length_ratio（修异常 0.03）；需全链路验证落在 0.8–1.2。

- [x] **O7 时间轴同步分 ≥0.65（C1）**（2026-08-27）  
  修复：元音点计入后再打分；无权重强制能量 VAD；拼音去调识别韵母；评分=覆盖40%+均匀35%+密度25%（峰值路径取高）。  
  冒烟：`scripts/smoke_o7_sync.py` → sync≈0.76；**全链路 run_154157 → sync=0.78**。  
  对比图 `outputs/o7_sync_before_after.png`。

- [ ] **O8 像度听测**  
  准备 **真实双人录音**（非 macOS `say` 合成），对比 edge / Qwen0.6B / Qwen1.7B 主观 MOS（1–5）。

### P2 · 验收材料（结题导向）

- [ ] **O9 标准测试集（D2）**  
  三类各 1 条：单人独白、双人对话、带噪/远场；固定 `summary` 指标表格。

- [ ] **O10 演示包**  
  输入 wav + 输出 wav + summary + 前后对比说明（1 页 PPT 口径）。

- [ ] **O11 仓库与文档**  
  README 增加「Mac + venv + Qwen3」一键说明；`01要求文档` 与本文档同步更新。

---

## 七、建议验收口径（更新）

| 指标 | 08-26 初版 | 08-27 午 | **08-27 验收** | 下一阶段目标 |
|---|---|---|---|---|
| 说话人数（双人样例） | 1 | 2 | **2** | 2，且少 retry |
| supports_clone | False | True | **True** | True |
| 输出可听完整度 | 2.1s | 16.6s | **17.9s** | 与源时长 ±20% |
| 译文语言 | 中文/错 | 英文 | **英文（在线）** | 保持 google 主路径 |
| length_ratio | — | 0.03 | **1.16** | 0.8–1.2 |
| 同步分 | 0.50 | 0.63 | **0.78** | 保持 ≥0.65 |
| 全链路耗时 | ~26s（无克隆） | ~80min | **~61s** | 保持 <5min |
| 克隆像度（主观） | 无 | 可演示 | **可演示** | 明显像原说话人 |
| 分离 degraded | — | True | **False** | False |
| 翻译后端 | 短语表 | 短语表 | **google 8/8** | 保持在线主路径 |

---

## 八、风险与边界

1. **Mac 无 NVIDIA GPU**：Qwen 推理慢，答辩现场需预生成或缩短样例。  
2. **演示样例为 TTS 合成音**：分离/克隆指标在真实录音上可能下降，需尽快补真实样例。  
3. **短语表仅离线兜底**：主路径已改为在线翻译；网络/API 全挂时仍回退短语表。  
4. **0.6B vs 1.7B**：像度与速度 trade-off，需实测后再定默认模型。

---

## 九、相关文件索引

| 文档/目录 | 说明 |
|---|---|
| `01要求文档.md` | 优化清单与优先级 |
| `docs-立项申请书.txt` | 立项目标原文 |
| `data/dialog_two_speakers_16k.wav` | 标准双人验收音频 |
| `outputs/run_20260827_144903/` | **当前最佳验收结果（~96s）** |
| `outputs/run_20260827_105928/` | 此前 Qwen 全链路（~80min） |
| `outputs/qwen3_clone_smoke.wav` | Qwen3 安装冒烟测试 |
| `scripts/setup_qwen3.sh` | Qwen3 环境脚本 |
| `config/default.yaml` | 当前默认配置 |

---

*下次更新建议：推进 O8 真实录音听测；可选稳定分离首轮（pyannote / F0）。*

---

## 十、本次优化变更（2026-08-27）

| 项 | 改动 |
|---|---|
| O1 | `.venv` 安装 deep_translator / nltk / edge-tts / opencc；`requirements.txt` 同步 |
| O2/O3 | `speaker_diarization._correct_turn_taking`；config 增加 turn/F0 参数 |
| O4 | pipeline 标记 `batch_mode`；修复 Mac 误用 `D:` 缓存目录 |
| O5 | 繁→简预处理 + Google/MyMemory 回退链；坏结果检测加强 |
| O7 | 能量VAD强制兜底；拼音韵母元音点；sync 重算；`smoke_o7_sync.py` + before/after 图 |
| ASR 碎句 | `_merge_broken_phrases`；config merge_gap/max；`smoke_asr_merge.py`；run_155909 |
| O6 部分 | length_ratio 改为逐段译文统计 |
| 冒烟 | ASR6段 + 2人 + turn_correct；短句 Google→Hello |
