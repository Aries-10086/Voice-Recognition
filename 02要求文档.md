# 02 要求文档 · 下一阶段优化清单

> 承接 [`01要求文档.md`](01要求文档.md)、[`01结果文档.md`](01结果文档.md)。  
> **基线（2026-08-27）**：`run_155909` — Qwen3 真克隆、Google 6/6、sync=0.78、ASR 碎句合并、~50s 全链路。  
> **本阶段主诉**：从「工程可演示」推进到「真实场景可验收、答辩可展示」。

---

## 一、01 已完成 vs 仍欠差距

| 申请书能力 | 01 状态 | 02 目标 |
|---|---|---|
| 多说话人分声 + 分人克隆 | 人数=2 ✅；标签偶发错、首轮常 retry ⚠️ | **首轮稳定分 2 人**；标签与轮次主观可用 |
| 跨语种翻译 | Google 在线 ✅；无短语表依赖 | **译文时长可控**；情感进翻译；少生硬句 |
| 时间轴 / 口型同步 | sync=0.78 ✅ | **保持 ≥0.65**；补口型演示素材 |
| 声纹像度 | 0.6B 可演示 ⚠️ | **真实录音 MOS ≥3.5**；择优 0.6B/1.7B |
| 验收材料 | summary 完整 ✅ | **三类测试集 + 演示包 + README** |

**当前最佳试听**：`outputs/run_20260827_155909/cloned_output.wav`

---

## 二、问题拆解（基于 run_155909）

### 问题 C：分离「人数对、标签不准」

1. 首轮常塌缩为 1 人，依赖 `retry_split` 才到 2 人（`spectral_cluster+retry`）
2. pyannote 未装，强分离能力未启用
3. `turn_correct` 后 degraded=False，但段级 SPEAKER_00/01 与真实 A/B 不完全一致
4. 演示音频为 macOS `say` 合成，F0/嵌入在真实录音上可能更差

### 问题 D：听感「能听、不够像、不够自然」

1. Qwen3 **0.6B-Base** 像度有限；1.7B 未对比
2. 情感多为 neutral，韵律平；停顿未跟源语对齐
3. `length_ratio=1.21` 略超 0.8–1.2，合成端可能微裁切/拖音
4. 个别译文仍偏直译（如 *sit down first*），缺上下文 refinement

### 问题 E：验收与答辩材料不足

1. 仅 1 条标准双人样例；缺单人/带噪/远场
2. 无固定「前后对比」一页纸与口型短视频
3. README 未覆盖 Mac + venv + Qwen3 一键复现
4. 真实双人录音 **零样本** 未测

---

## 三、优化清单（按优先级）

### P0 · 真实可用（2 周内，阻塞中期验收）

- [x] **F1 说话人分离首轮稳定**（2026-08-28）  
  多候选择优（F0 / spectral / sensitive）；`first_round_speakers` + `used_retry` 写入 summary。  
  冒烟：`f0_kmeans` 首轮 2 人、`used_retry=False`。可选：`scripts/setup_pyannote.sh` + `HF_TOKEN`。

- [x] **F2 段级标签与参考音对齐**（2026-08-28）  
  KMeans 嵌入重标 + `label_confidence`；`segments.json` / summary 可观测。  
  冒烟：avg_conf≈0.99；轮次标签 `00,01,00,01,00,00`（末段 ASR 切分仍影响）。

- [ ] **F3 真实录音验收（O8）**  
  规范见 `data/README.md`；已有 `real_dialog_01.wav`（SLR162），**MOS 表待填**。

### P1 · 效果提升（中期答辩前）

- [x] **F4 时长/音节约束（O6 / C2）**（2026-08-28）  
  - 翻译：`_constrain_length_ratio` 压到 **0.8–1.2**（去填充 + 音节预算截断）  
  - 合成：批处理路径按源组 `time-stretch`；整轨压到源时长 ×[0.8,1.2]  
  - 验收：`run_162658` length_ratio=**0.98**；输出 13.2s / 源 15.8s ≈0.84；sync=0.78  
  - 冒烟：`python scripts/smoke_f4_length.py`

- [x] **F5 克隆像度选型（0.6B vs 1.7B）**（2026-08-28）  
  - 同参考音对比脚本：`bash scripts/compare_qwen_models.sh`  
  - 结果：`outputs/f5_compare/report.md`（0.6B clone 10.2s / 1.7B 13.3s；MFCC 代理 0.994 vs 0.986）  
  - **默认保持 `Qwen3-TTS-12Hz-0.6B-Base`**（Mac/MPS 保速；1.7B 已缓存可改 config）  
  - 主观 MOS 表在 report.md，待听测填写

- [x] **F6 情感与韵律**（2026-08-31）  
  - 段级情感提前到翻译前；Google 路径情感措辞；同情绪参考音加分  
  - Qwen 克隆后轻度 pitch/tempo hint；组间 gap 对齐源语静音（`align_source_gaps`）  
  - 冒烟：`python scripts/smoke_f6_prosody.py`

- [x] **F7 译文质量 refinement**（2026-08-31）  
  - 错译黑名单（sit down / Taiqing）+ 术语表 + 短语表兜底  
  - 短句合并后再译（同说话人短间隔）  
  - 验收：无 *sit down first* / *Taiqing*；冒烟 `scripts/smoke_f7_refine.py`

### P2 · 验收材料（结题导向）

- [ ] **F8 标准测试集（O9 / D2）**  
  - 三类各 **≥1 条**，固定命名与 `data/README.md` 说明：  
    1. 单人独白 `solo_*.wav`  
    2. 双人对话 `dialog_*.wav`（含现有 `dialog_two_speakers_16k.wav`）  
    3. 带噪/远场 `noisy_*.wav`  
  - 每类跑通后填 **固定指标表**（人数、sync、length_ratio、耗时、引擎）

- [ ] **F9 口型同步演示（C4）**  
  - 准备 **≥15s** 带人脸视频 + 源语音；导出「约束前/后」时间轴对比  
  - 可复用 `outputs/o7_sync_before_after.png` 口径，补 1 段短视频或 GIF

- [ ] **F10 演示包（O10）**  
  - 1 页 PPT 口径：`输入 wav` → `输出 wav` → `summary 关键数` → `前后听感一句话`  
  - 目录：`docs/demo_pack/`（音频 + 图 + 说明 md）

- [ ] **F11 仓库与文档（O11）**  
  - `README.md`：**Mac + Python 3.11 venv + Qwen3** 一键命令；模型缓存路径；常见报错  
  - 同步更新 `01/02` 要求与结果文档索引

---

## 四、建议验收指标（02 阶段）

| 指标 | 当前（run_162658 / F4） | 02 中期底线 | 02 结题目标 |
|---|---|---|---|
| 说话人数（双人） | 2，retry=False | 2，retry <30% | 2，标签主观可用 |
| 分离 degraded | False | False | False |
| supports_clone | True | True | True |
| 翻译后端 | google + length refine | google 主路径 | + refinement，无硬伤译句 |
| length_ratio | **0.98** | **0.8–1.2** | 0.9–1.1 |
| 输出/源时长 | 13.2/15.8 ≈0.84 | ±20% | ±15% |
| 同步分 | 0.78 | **≥0.65** | ≥0.70 |
| 全链路耗时 | ~61s | <5min | <3min（同机） |
| 克隆 MOS（像度） | 未测（真实样例待填） | **真实录音 ≥3.0** | **≥3.5** |
| 测试集覆盖 | 双人 TTS + real_dialog_01 | 3 类各 1 | 3 类 + 指标表固定 |
| 演示材料 | 无 | 演示包初版 | PPT + 可选口型视频 |

---

## 五、建议落地顺序（4 周节奏）

| 周 | 重点 | 产出 |
|---|---|---|
| W1 | **F3** 真实录音 + **F1** pyannote 尝试 | 1 条真实 wav + 分离首轮对比 log |
| W2 | **F2** 标签对齐 + **F4** length_ratio | run 记录 length_ratio∈[0.8,1.2] |
| W3 | **F5** 0.6B/1.7B + **F6/F7** 听感与译文 | MOS 表 + 默认模型决策 |
| W4 | **F8–F11** 测试集与演示包 | `docs/demo_pack/` + README |

**并行可做**：`scripts/smoke_asr_merge.py`、`smoke_o7_sync.py` 纳入 CI 或 `make smoke`（可选）。

---

## 六、明确不纳入 02（防发散）

- 不自研 ASR/TTS/分离大模型训练  
- 不追求商用级「以假乱真」像度  
- 视频口型 **像素级** 驱动不阻塞；F9 仅需「时间轴约束可展示」  
- 多语种扩展（日/韩/法）留到结题后，02 以 **zh→en** 为主

---

## 七、建议验证命令

```bash
cd voiceclone
source .venv/bin/activate
export HF_HOME="$(pwd)/models/huggingface"
export NLTK_DATA="$(pwd)/models/nltk_data"

# 标准双人验收
python run_pipeline.py --input data/dialog_two_speakers_16k.wav --target-lang en --device cpu

# 冒烟（改 ASR / 同步后必跑）
python scripts/smoke_asr_merge.py
python scripts/smoke_o7_sync.py

# 看关键指标
cat outputs/run_*/summary.txt | tail -20
```

**通过线速查**：`说话人数≥2` · `supports_clone=True` · `sync≥0.65` · `length_ratio 0.8–1.2` · 无 Taiqing 类误译

---

## 八、预期改动文件（02）

| 文件 | 方向 |
|---|---|
| `src/asr/speaker_diarization.py` | pyannote、首轮稳定、段级重标 |
| `src/integration/pipeline.py` | 标签置信度、时长约束贯通 |
| `src/translation/llm_translator.py` | 音节/时长 refine、术语后处理 |
| `src/synthesis/tts_engine.py` | 1.7B 切换、韵律 hint |
| `config/default.yaml` | pyannote、length_ratio、默认 Qwen 型号 |
| `data/` | 真实录音 + 三类测试集 |
| `docs/demo_pack/` | 演示包（新建） |
| `README.md` | Mac 一键复现 |
| `02结果文档.md` | 每轮验收记录（待建） |

---

## 九、与 01 文档的对应关系

| 01 项 | 02 承接 |
|---|---|
| O1–O5、O7、ASR 碎句 | ✅ 已完成，02 仅回归验证 |
| O6 | → **F4** |
| O8 | → **F3** + **F5** |
| O9 | → **F8** |
| O10 | → **F10** |
| O11 | → **F11** |
| A6/B6 听测 | → **F3/F5** MOS |
| C2/C4 | → **F4/F9** |
| D2–D4 | → **F8–F11** |

---

| 项 | 改动 |
|---|---|
| F1 | `_pick_offline_labeling` 多候选择优；`first_round_speakers` / `used_retry` |
| F2 | `_refine_labels_by_embedding`；`label_confidence` → segments.json |
| F3 部分 | `data/README.md` 录制规范；`scripts/setup_pyannote.sh` |
| 冒烟 | `scripts/smoke_diarization.py` |

